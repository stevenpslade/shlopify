<header>
  <div class="max-w-7xl mx-auto px-16 sm:px-4 md:px-8">
    <% if yield.presence %>
      <%= yield %>
    <% else %>
      <h2 class="text-2xl font-bold leading-tight text-gray-900">
        <%= header_body %>
      </h2>
    <% end %>
  </div>
</header>