<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application','https://rsms.me/inter/inter.css', media: 'all', 'data-turbolinks-track': true %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'store/application', 'data-turbolinks-track': 'reload' %>

    <%# Future Theme style import will look like this below: %>
    <%# stylesheet_pack_tag '#{@theme}/application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body <%= yield(:body_attributes) %>>
    <div class="min-h-screen bg-gray-50">
      <%= render "components/#{@theme_path}/navigation/navbar" %>
      <%= yield %>

      <%# Cart (eventually will be a component) %>
      <div
        id="cart"
        data-turbolinks-permanent
        class="bg-gray-50 fixed inset-y-0 right-0 w-full md:w-2/5 flex flex-col"
        x-data="{
          open: false,
          cart: <%= cart_json %>
        }"
        x-show="open"
        @open-cart.window="open = $event.detail.open"
        @click.away="open = false"

        @add-product.window="
          const data = $event.detail;
          cart['products'] = { ...cart['products'], ...data.product };
          cart['final_price'] = data.final_price;
        "
      >
        <div class="h-16 flex justify-center items-center">
          <div class="absolute left-0 flex">
              <button
                class="inline-flex rounded-md text-gray-900 focus:outline-none transition ease-in-out duration-150"
                @click="open = false"
              >
                <svg class="h-full w-8" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"/>
                </svg>
              </button>
          </div>
          <h3 class="text-2xl font-bold leading-tight">Cart</h3>
        </div>

        <template x-for="(product, index) in Object.values(cart.products)" :key="index">
          <span x-text="product.title + ' x ' + product.quantity"></span>

        </template>

        <span x-text="cart.final_price"></span>
      </div>

      <div class="fixed bottom-0 left-0 bg-white shadow sm:rounded-lg">
        <div class="px-4 py-5 sm:p-6">
          <div class="mt-2 sm:flex sm:items-start sm:justify-between">
            <div class="max-w-xl text-sm leading-5 text-gray-500">
              <h3>
                Added to Cart
              </h3>
            </div>
            <div class="mt-5 sm:mt-0 sm:ml-6 sm:flex-shrink-0 sm:flex sm:items-center">
              <span class="inline-flex rounded-md shadow-sm">
                <button
                  type="button"
                  class="inline-flex items-center px-4 py-2 border border-transparent text-sm leading-5 font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-500 focus:outline-none focus:border-indigo-700 focus:shadow-outline-indigo active:bg-indigo-700 transition ease-in-out duration-150"
                  x-data="{ open: true }"
                  @click="$dispatch('open-cart', { open })"
                >
                  Open Cart
                </button>
              </span>
            </div>
          </div>
        </div>
      </div>

      <%= render "components/#{@theme_path}/navigation/footer" %>
    </div>
  </body>
</html>
