<%= form_with(model: admin_product_image, local: true) do |form| %>
  <% if admin_product_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_product_image.errors.count, "error") %> prohibited this admin_product_image from being saved:</h2>

      <ul>
      <% admin_product_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
