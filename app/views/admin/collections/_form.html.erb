<%= form_with(model: admin_collection, local: true) do |form| %>
  <% if admin_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_collection.errors.count, "error") %> prohibited this admin_collection from being saved:</h2>

      <ul>
      <% admin_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
